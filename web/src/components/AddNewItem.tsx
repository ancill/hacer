import { AddButton } from './AddButton';
import { Input } from './Input';

export const AddNewItem = () => {
  return (
    <div className="ml-3">
      <Input />
      <AddButton />
    </div>
  );
};
